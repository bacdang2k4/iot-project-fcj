# --- 1. Terraform ---
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
*.tfvars
!example.tfvars
.terraform.lock.hcl
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# --- 2. Java / Maven (Backend) ---
target/
*.jar
*.war
*.ear
*.class
.mvn/
mvnw
mvnw.cmd

# --- 3. Firmware / ESP32 (Phần mới thêm) ---
# Thư mục build của PlatformIO (Rất nặng, không được push)
.pio/
.pioenvs/

# File chứa Key nhạy cảm (TUYỆT ĐỐI QUAN TRỌNG)
**/secrets.h
**/wifi_creds.h

# File biên dịch ra binary
*.bin
*.elf
*.hex

# File cấu hình C++ cho Neovim/Clangd (Nó chứa đường dẫn máy local của bạn)
compile_commands.json
.clang-format

# --- 4. IDE / Editor ---
.idea/
*.iml
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db